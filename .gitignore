# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/

# Environments
.env
.venv
env/
venv/
ENV/
ribo-mgo-env/

# Jupyter
*.ipynb_checkpoints

# Editors / IDEs
.vscode/
.idea/
.obsidian/
*.swp
*.swo

# OS and file-manager junk
.DS_Store
._*
Thumbs.db
*.directory
.Trash-*/
.fuse_hidden*
.nfs*

# Transient
*~
*.bak
*.backup
*.log
*.tmp
*.temp
*.lock

# Local-only data. Deliberately untracked -- see CLAUDE.md. Do not "fix" this
# by adding data to git.
00-data-raw/
00-full-text/
rp-script/

# Superseded by mgo-adduct-extract/; kept locally for reference only.
extract-scripts/

# Raw UniProt candidate sets (~2.7 MB). Useful locally for auditing why a row
# scored the way it did, but regenerable -- rerun run_uniprot_name_match.py
# with --candidates-output. The mapping itself and the review files are tracked.
*.candidates.jsonl

# Downstream analyses. Local-only for now; all three rebuild from
# 04-data-merged/mgo-glycated-proteins.tsv.
05-analysis-upset/
06-analysis-go/
07-analysis-string/